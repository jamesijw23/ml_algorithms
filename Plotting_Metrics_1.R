library(ggplot2)

test_df = data.frame(metric1 = c(0.25,0.3), metric2 = c(-0.4,-0.2),levelnames=c("cat","dog"))

ggplot(test_df,aes(x=))













animals = c("dog","cat","fish")

tr = animals[sample(3,100,replace = T)]
es = animals[sample(3,100,replace = T)]
metrics(tr,es)
colnames(d)
